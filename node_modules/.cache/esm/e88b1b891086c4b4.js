_108‍.x([["sendMessage",()=>sendMessage],["listenToChat",()=>listenToChat]]);var lastMessage = {};

       async function sendMessage(req, res, io) {
	console.log("Hitting this");

	const message = req.query.message;
	if (lastMessage != message) {
		lastMessage = {
			text: message,
			user: req.query.user
		};
	}

	console.log("Testing");

	res.send({success: true});
}

       async function listenToChat(req, resp) {

}